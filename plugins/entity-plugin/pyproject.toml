[project]
name = "mkdocs-entity-plugin"
version = "0.1.0"
description = "An MkDocs plugin for entity references"
requires-python = ">=3.10"
dependencies = [
    "mkdocs"
]

[project.entry-points."mkdocs.plugins"]
entity = "mkdocs_entity_plugin.plugin:EntityPlugin"